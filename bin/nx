#!/usr/bin/env sh

set -eu

case $(dirname "$0") in
    /*|./*) Dir=$(cd "$(dirname "$0")" && pwd);;
    *) Dir=$(cd "$PWD/$(dirname "$0")" && pwd);;
esac

RepositoryDir=$(cd "$Dir/.." && pwd)

export PATH="$RepositoryDir/node_modules/.bin:$RepositoryDir/bin:$PATH"

exec nx "$@"
